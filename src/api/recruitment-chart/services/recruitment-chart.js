'use strict';

/**
 * recruitment-chart service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::recruitment-chart.recruitment-chart');
